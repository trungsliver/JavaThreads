================ Luồng Daemon và Luồng Người Dùng trong Java =======================
🔹 Phân loại luồng theo cách thực thi:
Trong Java, dựa trên cách thức hoạt động, luồng có thể chia làm 2 loại:
        - Luồng Daemon (Daemon Thread)
        - Luồng Người Dùng (User Thread)

🔹 Luồng chính (Main Thread)
    - Khi một chương trình Java bắt đầu chạy, luồng chính (luồng chạy từ phương thức main()) sẽ được khởi động đầu tiên.
    - Từ luồng chính, ta có thể tạo và khởi động các luồng con khác.
    - Trong điều kiện bình thường, luồng chính là luồng cuối cùng kết thúc, vì nó phải thực hiện một số thao tác dọn dẹp (shutdown) trước khi chương trình kết thúc.

🔹 Luồng Daemon
    - Là các luồng chạy nền, thường có độ ưu tiên thấp.
    - Chúng không thực hiện công việc chính, mà hỗ trợ cho các luồng người dùng.
    - Ví dụ điển hình: luồng thu gom rác (GC thread) trong JVM là một luồng daemon.
    - JVM sẽ tự động dừng các luồng daemon khi tất cả các luồng người dùng đã kết thúc.

🔹 Luồng Người Dùng (User Thread)
    - Là những luồng do lập trình viên tạo ra để thực hiện công việc chính của chương trình.
    - JVM chờ các luồng người dùng hoàn thành công việc trước khi kết thúc chương trình.
    - Khi một luồng người dùng kết thúc, JVM sẽ kiểm tra xem có còn luồng người dùng nào đang chạy không.
    - Chỉ khi tất cả luồng người dùng kết thúc, JVM mới dừng các luồng daemon còn lại và kết thúc chương trình.

🔹 Tóm lại:
    - Luồng người dùng được ưu tiên hơn và sẽ chạy đến khi hoàn tất công việc.
    - Luồng daemon chỉ tồn tại để hỗ trợ và sẽ bị JVM dừng lại khi không còn luồng người dùng nào.